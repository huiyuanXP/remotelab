{
  "name": "test",
  "description": "Test workflow: ping all workspaces",
  "steps": [
    {
      "id": "ping",
      "type": "parallel",
      "tasks": [
        {
          "id": "ping-project",
          "workspace": "~/my-project",
          "model": "haiku",
          "prompt": "Output one line: test"
        }
      ]
    }
  ]
}
